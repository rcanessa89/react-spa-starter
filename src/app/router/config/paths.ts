const root = '/';
const home = '/home';
const test = '/test';
const publicRouteRedirect = home;
const protectedRouteRedirect = root;
const noMatch = '/404';

export default {
  home,
  noMatch,
  protectedRouteRedirect,
  publicRouteRedirect,
  root,
  test,
};
